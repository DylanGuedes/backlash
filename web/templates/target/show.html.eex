<h1><%= @target.name %></h1>

<h2>Setups</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <%= for stp <- @target.setups do %>
      <tr>
        <td><%= link(stp.name, to: setup_path(@conn, :show, stp.id)) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
